# Repo-root file for Docker Compose interpolation only (copy to .env beside docker-compose.yml).
# Application secrets live in app/backendapi/.env and app/agents/.env.

API_PORT=8000
FEEDBACK_AGENT_PORT=5055

# Path on the VM/host mounted read-only into api/worker as /run/secrets/google-credentials.json
GOOGLE_CREDENTIALS_FILE_HOST=./credentials.json

# Uvicorn workers (read inside the api container from env_file or here via compose merge)
WEB_CONCURRENCY=2

# Cloud SQL Auth Proxy (--profile cloudsql). No public-IP authorized networks required.
# CLOUD_SQL_CONNECTION_NAME=athletefocus-agents:us-central1:your-instance

# ---- Local dev only (docker-compose.dev.yml) ---------------------------------
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=strongpassword
# POSTGRES_DB=athlete
# POSTGRES_PORT=5432
# DEV_HOT_RELOAD=1
