fastapi==0.115.12
uvicorn[standard]==0.34.2
python-dotenv==1.0.1
pydantic==2.11.4
openai==1.76.0
Pillow==11.2.1
opencv-python-headless==4.11.0.86
jinja2==3.1.6
python-multipart==0.0.20
pytesseract==0.3.13
pyyaml>=6.0
# Highlight overlay detection (YOLOv8). Torch is pulled by ultralytics; we pin the
# CPU build explicitly via --extra-index-url in the Dockerfile to keep the image small.
ultralytics==8.3.27
torch==2.4.1
